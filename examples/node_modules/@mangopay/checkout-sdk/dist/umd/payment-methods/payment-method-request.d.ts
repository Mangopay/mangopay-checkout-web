import { AuthorizeApplePayResult } from '@mangopay/sdk-authorize';
export interface PaymentMethodRequest {
    isActive: boolean;
    start(): void;
    abort(): void;
    onComplete?: (result: AuthorizeApplePayResult) => void;
    onFailed?: () => void;
}
