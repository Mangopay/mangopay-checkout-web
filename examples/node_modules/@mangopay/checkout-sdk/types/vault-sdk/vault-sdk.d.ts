import { VaultSdkOptions } from './vault-sdk-options';
import { CardInfoObject, PreregistrationData } from './tokenize-payment-method';
import { UpdateCardRegistrationOutput } from './update-card-registration';
export declare class VaultSdk {
  private clientId;
  private environment;
  constructor(options: VaultSdkOptions);
  tokenizePaymentMethod(
    cardInfoObject: CardInfoObject,
    preregistrationData: PreregistrationData
  ): Promise<UpdateCardRegistrationOutput>;
}
