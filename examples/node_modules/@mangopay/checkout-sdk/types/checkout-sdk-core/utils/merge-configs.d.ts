import { ConfigProperty } from './config-property';
import { DeepPartial } from './deep-partial';
export declare const mergeConfigs: <T extends Record<string, ConfigProperty>>(
  defaultConfig: T,
  configOverrides: DeepPartial<T> | undefined
) => T;
