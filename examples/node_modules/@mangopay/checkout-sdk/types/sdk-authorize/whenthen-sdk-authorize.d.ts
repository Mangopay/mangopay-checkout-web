import { SdkAuthorizeOptions } from './sdk-authorize-options';
import { CreateCardDirectPayInInput, CreateCardDirectPayInResult } from './create-card-direct-pay-in';
import { ViewCardInput, ViewCardResult } from './view-card';
import { ViewPayInInput, ViewPayInResult } from './view-pay-in';
export declare class SdkAuthorize {
  private readonly clientId;
  private readonly environment;
  constructor(options: SdkAuthorizeOptions);
  viewCard(data: ViewCardInput): Promise<ViewCardResult>;
  createCardDirectPayIn(data: CreateCardDirectPayInInput): Promise<CreateCardDirectPayInResult>;
  viewPayIn(data: ViewPayInInput): Promise<ViewPayInResult>;
}
